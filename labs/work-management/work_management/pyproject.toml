[project]
name = "work_management"
authors = [
    { name = "VEQTA" },
]
description = "Unified operational work management for Frappe Framework"
requires-python = ">=3.14,<3.15"
readme = "README.md"
dynamic = ["version"]

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
frappe = ">=16.0.0,<17.0.0"

[tool.ruff]
line-length = 110
target-version = "py314"

[tool.ruff.format]
quote-style = "double"
indent-style = "tab"
