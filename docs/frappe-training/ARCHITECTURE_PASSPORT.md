# Архитектурный паспорт учебного практикума Frappe

Статус: **черновик архитектуры для согласования**.

Этот документ фиксирует основу нового учебного практикума по Frappe Framework 16. Практикум не связан с VEQTA, не использует её предметную область и не наследует решения предыдущих версий.

Нормативная база — [`docs/frappe-architecture-standard`](../frappe-architecture-standard/README.md). Если учебное решение противоречит стандарту, исправляется практикум, а не стандарт подгоняется под занятие.

Связанные документы:

- [`REQUIREMENTS_MATRIX.md`](REQUIREMENTS_MATRIX.md) — требования и выбор механизмов;
- [`STAGE_DEPENDENCY_GRAPH.md`](STAGE_DEPENDENCY_GRAPH.md) — реальные зависимости между практическими результатами.

---

## 1. Цель

Практикум должен научить **строить небольшое приложение так, как предполагает сам Frappe**, а не проходить каталог функций Framework.

Для каждого требования ученик должен уметь ответить:

```text
1. Какая ответственность появилась?
2. Какой штатный механизм Frappe уже отвечает за неё?
3. Совпадает ли его смысл с требованием?
4. Если нет — какая официальная точка расширения подходит?
5. Какую новую ответственность действительно добавляет собственный код?
```

Основание:

- [`01_FOUNDATIONS.md`](../frappe-architecture-standard/01_FOUNDATIONS.md);
- [`10_DECISION_STANDARD.md`](../frappe-architecture-standard/10_DECISION_STANDARD.md);
- [`12_EVIDENCE_REGISTER.md`](../frappe-architecture-standard/12_EVIDENCE_REGISTER.md).

---

## 2. Чем практикум не является

Это не:

- энциклопедия всех возможностей Frappe;
- последовательность «урок DocType → урок Workflow → урок Report»;
- курс no-code;
- курс Python;
- демонстрация VEQTA;
- попытка обязательно использовать каждый механизм Framework;
- набор искусственных требований ради очередной функции.

Наличие кода само по себе не считается плохой архитектурой. Плохой признак — собственный механизм, который без причины дублирует уже существующую ответственность Frappe.

---

## 3. Учебная предметная область

Практикум строится вокруг **небольшого сервиса проката оборудования**.

Предметная область без искусственного усложнения даёт:

- самостоятельные записи;
- Document с несколькими строками;
- связи между Documents;
- рабочее состояние;
- серверные инварианты;
- роли и права;
- возможность позже добавить реальные ветки настроек, печати, отчётов, уведомлений, внешнего ввода и API.

Ни одна дополнительная ветка не становится обязательной только потому, что соответствующий механизм существует во Frappe.

---

## 4. Минимальная CORE-модель

```text
Equipment
Customer
Rental
└── Rental Item [Child DocType]
```

### Equipment

Самостоятельный объект: имеет идентичность, существует независимо от одной выдачи, используется многими Rentals и открывается отдельно.

### Customer

Самостоятельный клиент: существует независимо от Rental, участвует во многих Rentals, а Rental ссылается на него через `Link`.

### Rental

Главный рабочий Document, объединяющий клиента, период, состав Equipment и предметное состояние.

`Rental` заранее **не** объявляется `Is Submittable` и не получает `Workflow`.

### Rental Item

Часть одного Rental, поэтому начальное решение — `Child DocType`.

Если позднее строка получает самостоятельную идентичность, permissions, lifecycle или независимые ссылки, модель пересматривается по [`02_DATA_MODEL.md`](../frappe-architecture-standard/02_DATA_MODEL.md).

---

## 5. Что не создаётся заранее

В CORE заранее нет:

```text
Equipment Type
Rental Settings
Rental Status
Priority
Rental History
Rental Comment
Rental Assignee
Approval
Notification Log
Attachment
Rental Request
```

Принцип:

- маленький стабильный тип → сначала `Select`;
- одна настройка всего Site → тогда кандидат `Single DocType`;
- обычная история → штатный `Version`/Timeline;
- комментарии → `Comment`;
- файлы → `File`/Attach;
- назначение → сначала `Assignment`/`ToDo`;
- status не требует отдельного справочника;
- несколько состояний не требуют автоматически `Workflow`;
- публичная форма не доказывает необходимость отдельного `Rental Request`.

Это не запрет. Это запрет вводить механизм **до появления его ответственности**.

---

## 6. Архитектурная карта среды

```text
Bench
├── Apps
└── Sites
     └── учебный Site
          └── установлен учебный App

Учебный App
└── Module
     ├── Standard DocTypes
     ├── Controllers
     ├── Reports при необходимости
     ├── Workspace при необходимости
     ├── fixtures / patches при необходимости
     └── другие штатные компоненты
```

`Bench` — среда управления.

`Site` — конкретный экземпляр с БД и конфигурацией.

`App` — устанавливаемый пакет кода и метаданных.

`Module` — штатная группировка внутри App, а не автоматически bounded context.

`DocType`/`Document` — центральная прикладная модель.

---

## 7. Как появляется новый механизм

```text
реальное требование
    ↓
ответственность
    ↓
штатный механизм Frappe
    ↓
совпадает ли семантика?
    ├─ да → использовать
    └─ нет → официальная точка расширения
                   ↓
              собственная новая ответственность
```

Нельзя вводить шаг формулировкой «теперь изучим Workflow».

Нормальная формулировка: «менеджер должен подтверждать выдачу, оператор не может выполнить этот переход; проверяем Workflow».

---

## 8. Модель данных

```text
обычное свойство           → DocField
самостоятельный Document   → обычный DocType
состав одного Document     → Child DocType
ссылка на существующий Doc → Link
одна конфигурация на Site  → Single DocType
```

Более сложные варианты появляются только после соответствующего требования.

---

## 9. Состояние и lifecycle

```text
бизнес-статус = что происходит с объектом
Workflow      = кто и при каких условиях может выполнить переход
docstatus     = Draft / Submitted / Cancelled транзакционного Document
```

`Rental = Returned` не означает автоматически Submitted.

`Is Submittable` появляется только при смысле зафиксированного факта, который нельзя свободно переписывать.

---

## 10. Размещение логики

```text
удобство формы                  → клиентский механизм
инвариант данных                → серверный путь
поведение собственного DocType  → Controller
реакция на чужой DocType        → официальная точка расширения
сложная операция нескольких Docs → service, если появилась отдельная ответственность
```

Критическое правило не считается реализованным, если оно существует только в Client Script или скрытой кнопке.

---

## 11. Права

Стартовая модель:

```text
User
Role
DocType Permissions
```

`Permission Level`, `Permission Type`, `If Owner`, `User Permission`, `Share`, permission hooks и собственная политика появляются только после соответствующего требования.

UI-скрытие не является защитой данных.

---

## 12. UI следует за моделью

```text
бизнес-объект
    ↓
DocType + fields + lifecycle + permissions
    ↓
подходящее представление
```

`Form` и `List` — нормальный интерфейс по умолчанию.

`Calendar`, `Report`, `Workspace`, `Web Form`, Portal или custom frontend появляются под реальное пользовательское требование.

Новая сущность не создаётся только ради нового экрана.

---

## 13. Site и App — разные владельцы состояния

Нужно различать локальную настройку конкретного Site и обязательное состояние устанавливаемого App.

Если Role, поле, DocType или конфигурация объявлены частью App, они не должны существовать только благодаря ручному действию на одном dev-site.

Для доставки используются штатные механизмы по смыслу: Standard DocType JSON, fixtures, exported customizations, patches и т. д.

---

## 14. Финальный критерий

```text
чистый совместимый Frappe Site
+ учебный App из Git
+ install-app
+ migrate
+ tests
= рабочее обязательное состояние приложения
```

Все обязательные элементы восстанавливаются без скрытого списка ручных действий.

Тестируются наши критические контракты, а не Framework без причины.

---

## 15. Критерий качества учебного шага

```text
Требование: что понадобилось?
Ответственность: что должна обеспечить система?
Штатный механизм: что предусмотрел Frappe?
Почему подходит: какая семантика совпала?
Граница: когда станет недостаточно?
Результат: что реально работает?
```

Если нельзя объяснить, зачем механизм появился, шаг не включается.

---

## 16. Порядок проектирования практикума

```text
ARCHITECTURE_PASSPORT.md
        ↓
REQUIREMENTS_MATRIX.md
        ↓
STAGE_DEPENDENCY_GRAPH.md
        ↓
дорожная карта
        ↓
подробные практические инструкции
        ↓
чистая установка и финальная приёмка
```

Количество занятий и названия лабораторных работ заранее не фиксируются.